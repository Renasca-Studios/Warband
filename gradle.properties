org.gradle.jvmargs=-Xmx2G
org.gradle.java.home=C:\\Program Files\\Java\\jdk-26.0.1
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Fabric Properties
# Compile target. 26.2 is not source-compatible with 26.1.2 — a single jar cannot serve
# both, so 1.4.0 is the final 26.1.2 release. See the local porting notes for the taxonomy.
minecraft_version=26.2
loader_version=0.19.3
loom_version=1.17.17

# Mod Properties
mod_version=1.5.0
maven_group=com.warband
archives_base_name=warband

# Dependencies
fabric_api_version=0.157.0+26.2
